# Container listen port (used by nginx inside container)
PORT=80

# Optional alternate env var name (IWAN) — if set, entrypoint will prefer this for the listen port
# You can set either PORT or IWAN. Example: IWAN=9009
IWAN=

# Host port to map to the container. Change this to expose a different host port.
HOST_PORT=8080
